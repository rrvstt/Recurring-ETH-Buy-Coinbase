# Lambda-specific requirements
# Note: Some packages may already be available in Lambda runtime
# Only include what's needed for the Lambda function

coinbase-advanced-py == 1.8.2
requests >= 2.31.0
urllib3 >= 2.2.2
PyYAML >= 6.0.1
cryptography >= 42.0.4
cffi
alphasquared-py >= 0.3.0
fear-and-greed-crypto >= 0.1.0
PyJWT >= 2.8.0
websockets < 14.0, >= 12.0
backoff >= 2.2.1
